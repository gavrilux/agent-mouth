{
  "name": "agent-mouth-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev:serve-http": "node apps/cli/dist/index.js serve-http",
    "npm:pack": "pnpm --filter ./apps/cli pack"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
